name: Keep Streamlit Alive

on:
  schedule:
    
    - cron: '0 */2 * * *'
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send HTTP request to webapp
        run: |
          curl -I https://bibilog-py.streamlit.app/
